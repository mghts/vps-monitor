name: Publish Docker Images

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Docker image tag, for example v0.1.0"
        required: true
        default: "v0.1.0"
      push_latest:
        description: "Also push the latest tag"
        required: true
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  publish:
    name: Build and push multi-arch images
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Docker Hub secrets
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          test -n "$DOCKERHUB_USERNAME" || (echo "Missing DOCKERHUB_USERNAME secret" && exit 1)
          test -n "$DOCKERHUB_TOKEN" || (echo "Missing DOCKERHUB_TOKEN secret" && exit 1)

      - name: Prepare image tags
        id: meta
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_PUSH_LATEST: ${{ inputs.push_latest }}
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "push" ]; then
            VERSION="${GITHUB_REF_NAME}"
            PUSH_LATEST="true"
          else
            VERSION="${INPUT_VERSION}"
            PUSH_LATEST="${INPUT_PUSH_LATEST}"
          fi

          {
            echo "version=${VERSION}"
            echo "server_tags<<EOF"
            echo "docker.io/${DOCKERHUB_USERNAME}/vps-monitor-server:${VERSION}"
            if [ "${PUSH_LATEST}" = "true" ]; then
              echo "docker.io/${DOCKERHUB_USERNAME}/vps-monitor-server:latest"
            fi
            echo "EOF"
            echo "agent_tags<<EOF"
            echo "docker.io/${DOCKERHUB_USERNAME}/vps-monitor-agent:${VERSION}"
            if [ "${PUSH_LATEST}" = "true" ]; then
              echo "docker.io/${DOCKERHUB_USERNAME}/vps-monitor-agent:latest"
            fi
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push server image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/server.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.server_tags }}
          cache-from: type=gha,scope=server
          cache-to: type=gha,mode=max,scope=server

      - name: Build and push agent image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/agent.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.agent_tags }}
          cache-from: type=gha,scope=agent
          cache-to: type=gha,mode=max,scope=agent

      - name: Summary
        run: |
          echo "Published:"
          echo "${{ steps.meta.outputs.server_tags }}"
          echo "${{ steps.meta.outputs.agent_tags }}"
